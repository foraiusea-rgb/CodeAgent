import { Metadata } from "next";
import WorkspaceClient from "./workspace-client";

export const metadata: Metadata = { title: "Workspace — CodeAgent" };

export default function ReviewPage() {
  return <WorkspaceClient />;
}
